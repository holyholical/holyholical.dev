/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',           // needed for static export
  images: {
    unoptimized: true,        // GitHub Pages doesn’t support next/image optimization
  },
  basePath: '/holyholical',
  assetPrefix: '/;

module.exports = nextConfig;

